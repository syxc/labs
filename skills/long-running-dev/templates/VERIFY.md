# 证据模板

<!-- 动手之前先定证据：做完没，拿什么证明？先把证据定出来再动手。 -->
<!-- 这份是模板，按任务挑需要的填，不用样样都写。 -->

## 给这个任务定清

### 做完什么样

<!-- 一句话，做成之后世界变成什么样。「X 达到 Y」，不是「优化 X」。 -->

### 拿什么证明

<!-- 具体到一条命令、一个数、一张图、一段输出。要能跑、能看、能读。 -->

### 不能改坏什么

<!-- 哪些东西现在是对的，做完还得是对的。 -->

### 只能动什么

<!-- 哪些文件、目录、资源在这个任务范围内。越界的留原样。 -->

### 做不下去了怎么办

<!-- 卡住的时候，带着「试过什么、卡在哪、下一步要什么」停下来问。 -->

### 要求多高

<!-- 逼出多少扎实的活。「每个改动都验证过」要求高，「生成清单」要求低。
     强标准 = 又清晰、要求又高。 -->

上面是想清楚要什么证据，下面是具体怎么拿——拿「拿什么证明」那一条，对到下面找手段。

---

## 能跑的（最可靠）

电脑直接判对错。

```bash
# 测试。占位符，换成项目实际的命令。
<test-command>

# 编译 + lint。
<build-and-lint>
```

通过：全绿，退出码 0。

## 能看的

眼见为实。改效果类的东西，跑测试验不出来。

- 前端改动 → 开浏览器看，或截屏对比。工具：`/skill browser-use` 或 chrome-devtools CLI
- 看什么：布局没乱、交互正常、关键页面肉眼过得去

## 能读的

- 出错了：把报错完整读一遍，看到根因
- 改完了：看 diff，确认只动了该动的地方

---

## 常见场景

- 前端改动 → 跑测试 + 开浏览器看
- 后端 API → 跑测试 + 编译检查
- 修 bug → 先复现，改完验证不复发
- 重构 → 跑测试，保证行为不变
- 性能 → 定一个具体的数（P99、LCP、延迟），改完对那个数
